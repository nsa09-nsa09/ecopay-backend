{
  "name": "ecopay-api-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "API-level E2E tests for the Ecopay business chain (register → room → join → pay → use), run over HTTP against a running backend.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:api": "playwright test",
    "test:ui": "playwright test --config=playwright.ui.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
